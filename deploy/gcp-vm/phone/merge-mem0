#!/data/data/com.termux/files/usr/bin/sh
# Merge the mem0 fork's upstream-sync PR #1, overriding the required checks
# (only failures: Turbopuffer tests that need API keys the fork doesn't have).
gh pr merge 1 --repo c10vis-poem/clovis-mem0-vingiaN --merge --admin
gh pr view 1 --repo c10vis-poem/clovis-mem0-vingiaN --json state -q '"mem0 PR #1: " + .state'
